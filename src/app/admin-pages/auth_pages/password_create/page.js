import React from 'react';
import Password_Create from "@/components/Pages/Auth_Pages/Password_Create";

const page = () => {
    return (
        <Password_Create/>
    );
};

export default page;