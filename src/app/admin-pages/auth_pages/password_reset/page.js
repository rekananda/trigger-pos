"use client";
import React from 'react';
import Password_Reset from "@/components/Pages/Auth_Pages/Password_Reset";

const page = () => {
    return (
        <Password_Reset />
    );
};

export default page;